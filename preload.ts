/** Preload script for Electron renderer process. */
// This file is intentionally empty - it runs before the renderer process loads.
// Add any preload logic here (e.g., contextBridge API exposure).
export {};
